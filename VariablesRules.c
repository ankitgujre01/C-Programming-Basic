#include<stdio.h>

int main(){
    int a;   //is allowed
    int b;  // is allowed
    int a b;   //wide space is not allowed 

    int a@b; //is not allowed
    // int a()Bc; is not allowed
    int a_b;  //its allowed
    int 4;   //its not allowed
    int ankit;
    int Ankit;
    //Both are different variables

    /*
    1. first character must be an albhabet or underscore(_);
    2. no coma, blanks allowed;
    3. No special symbol other than (_);
    4. Variable names are case sensitive.
    */
}